var a;
var b;

a = 10;
b = 5;
var sum;

sum = a+b;
// print out the value of sum on the default console
/* on line 11 we call the log method on the console object
  and pass sum as the argument to the log method 
  the dot (.) oparator is used when calling (inoking) a method
  or a functon */
console.log('The value of sum is', sum)

var c, d;
c = 7,d = 4;

// multiply c and d
var product = c * d;
console.log('The product of c and d is', product);
var x = 6;
var div;
div = c / d;
var sub;
sub = d - a;
product = a * b;
sum = x + c;
var mod;
mod = c % b;
product = x * c;
console.log('the modulo of c and b is', mod);
console.log('the dividend of c and d is', div);
console.log('the subtraction of d and a is', sub);
