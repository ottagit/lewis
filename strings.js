// string munipulation using methods

// declare a string object
var name = 'lewis';
// capitalize the entire string object
var capital = name.toUpperCase();
console.log('converting name to capital gives', capital);
var name2 = 'LEWIS';
var lower = name2.toLowerCase();
console.log('converting name2 to lower case gives', lower);
// indexing in strings.
var first_index = name.indexOf('l');
console.log('the index of the first character of the string object name is', first_index);
